# 主动消息费用说明

主动消息候选筛选、用户上限检查、全局预算检查、免打扰检查和相关性评分均在本地完成，不调用模型，也不扣额度。

只有候选通过全部本地规则、正式进入模型生成阶段时，才预留 10 额度；成功生成后扣除，调用失败则自动释放。

用户设置的每日上限是“最多允许进入模型生成的次数”。实际次数可能因为全局预算、冷却、免打扰、余额不足或相关性不足而更少。

旧数据库需要先执行一次 `npm run db:fix-proactive`，放宽历史遗留的主动间隔和每日次数检查约束。该修复不涉及余额或订单数据。
