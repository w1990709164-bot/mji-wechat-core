# 主动陪伴测试备注

设置查询和本地筛选不会调用模型。候选通过全部规则并进入生成阶段后，按正常回复预留 10 额度；成功后扣除，失败自动释放。

旧数据库的 `wake_preferences` 表曾限制：

- 主动间隔最少 15 分钟
- 每天主动消息最多 50 次

拉取本分支后，需要先停止机器人并执行一次：

```powershell
npm run db:fix-proactive
```

该命令只放宽主动消息偏好表的检查约束，不修改用户、余额、订单、聊天或人设数据。执行成功后可重新启动机器人，并测试 `主动间隔 1分钟`。
